//document.go
/*简单IPC框架
1.简单ipc（进程间通信）框架的目的：封装通信包的编码细节，让使用者可以专注于业务。
2.采用channel作为模块之间的通信方式。虽然channel可以传递任何数据类型，甚至包括另外一个channel
但是为了让我们的架构更容易拆分，我们还是严格限制了只能用于传递json格式的字符串类型数据。
3.想将改这样的单进程实例修改为多进程的分布式架构，不需要全部重写，只需要替换通信层。
*/
package ipc
